version: 1.3.16

cache: true

endpoints:
  custom:
    - name: "Google Gemini (Live Server)"
      apiKey: "${GEMINI_API_KEYS}"
      baseURL: "https://my-ai-17x4.vercel.app/v1"
      models:
        default: []
        fetch: true
      titleConvo: true
      modelDisplayLabel: "Gemini"
